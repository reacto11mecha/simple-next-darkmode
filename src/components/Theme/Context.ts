import React from "react";
import type { contextInterface } from "./types";

// eslint-disable-next-line @typescript-eslint/no-non-null-assertion
export const DarkModeContext = React.createContext<contextInterface>(null!);
